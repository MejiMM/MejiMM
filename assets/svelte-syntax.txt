{#if condicion}
   //contenido

   {:else if condicion 2}
     //contenido 2

   {:else}
     //contenido defecto
{/if}

{#each array as element, [index]}
   //codigo para recorrer arreglo

   {:else}
      //contenido para mostrar si el arreglo esta vacio
{/each}

{#each array as element, [index]}
   //codigo para recorrer objeto
   //element.clave --> de lo contrario svelte mostrara                        object
{/each}

<#svelte:head>
   //codigo para modificar elementos del head
</svelte:head>

{@html ...} --> puede renderizar codigo html desde una variable

$: .... --> declara una variable reactiva, para que cuando cambie su valor, se renderice el DOM

<input bind:value={variable}> 
<select bind:value={variable}>
//actualizara la variable cada vez que se ingrese un valor

{beforeUpdate} from "svelte" //ejecuta el codigo antes de montar el componente
{onMount} from "svelte" //ejecuta codigo cuando el componente se monta
{onDestroy} from "svelte" //ejecuta codigo cuando es destruido
{afterUpdate} from "svelte" //ejecuta codigo luego de que ocurra un cambio
{tick} from "svelte" //ejecuta codigo cuando todos los demas se ejecutaron

